This project is a little chatbot demo, bootstrapped with [Create React App](https://github.com/facebookincubator/create-react-app).

To run the chatbot:

* Clone the repo
* Run `yarn install`
* Run `yarn start`